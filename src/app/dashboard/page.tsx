import React from 'react';
import { useQuery } from '@tanstack/react-query';
import { fetchUserData } from '../../config/fetch';

const Dashboard = () => {

  return (
    <div>
      Dashboard
      
    </div>
  );
};

export default Dashboard;
